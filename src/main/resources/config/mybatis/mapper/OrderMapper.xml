<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.order.mapper.OrderMapper">

	<!-- 주문명세서 : 주문번호(pk)가 생성된다 -->
	<insert id="createOrderList" parameterType="Order">
		<selectKey keyProperty="desc_oidx" resultType="int" order="BEFORE">
			select order_desc_seq.nextval from dual
		</selectKey>
		insert into order_desc(desc_oidx, midx, desc_oTotalPrice, delivaryState,
		payment, gradePoint, gradeDiscount, ocomment, desc_odate)
		values(#{desc_oidx}, #{midx}, #{desc_oTotalPrice}, '0',
		#{payment}, #{gradePoint}, #{gradeDiscount}, #{ocomment:VARCHAR}, sysdate)
	</insert>
	
	<!-- 수령자 -->
	<insert id="createOrderMember" parameterType="Order">
		<selectKey keyProperty="omidx" resultType="int" order="BEFORE">
			select ordermember_seq.nextval from dual
		</selectKey>
		insert into ordermember(omidx, desc_oidx, omname, omhp1, omhp2, omhp3,
		ompost, omaddr1, omaddr2, omdate)
		values(#{omidx}, #{desc_oidx}, #{omname}, #{omhp1}, #{omhp2}, #{omhp3},
		#{ompost}, #{omaddr1}, #{omaddr2}, sysdate)		
	
	</insert>
	
	<!-- 주문상품정보 -->
	<insert id="createOrderProductList" parameterType="OrderProduct">
		<selectKey keyProperty="oidx" resultType="_int" order="BEFORE">
			select orders_seq.nextval from dual
		</selectKey>
		insert into orders(oidx, desc_oidx, pidx, oqty, osalePrice, opoint, odate)
		values(#{oidx}, #{desc_oidx}, #{pidx}, #{oqty}, #{osalePrice}
		, #{opoint}, sysdate)
	</insert>
	
	<select id="getOrderList" parameterType="int" resultType="Order">
		select * from order_desc where midx =#{value} order by desc_oidx desc
	</select>
	
	<select id="getOrderProduct" parameterType="int" resultType="OrderProduct">
		select * from product where pidx=#{value}
	</select>
	
	<select id="getOrderProductList" parameterType="int" resultType="OrderProduct">
		select * from product join orders using(pidx) where pidx in (select pidx from orders where desc_oidx=#{value}) and desc_oidx=#{value}
	</select>
	
	<select id="getOrderDesc" parameterType="int" resultType="Order">
		select order_desc.*, decode(payment,1,'신용카드',2,'무통장입금',3,'휴대폰결제') payStr
		from order_desc where desc_oidx=#{value}
	</select>
	
	<select id="getOrderMember" parameterType="int" resultType="Order">
		select * from ordermember where desc_oidx=#{value}
	</select>
		
</mapper>