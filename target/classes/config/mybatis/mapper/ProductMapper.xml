<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.product.mapper.ProductMapper">
	<insert id="productInsert" parameterType="Product">
		<selectKey keyProperty="pidx" resultType="int" order="BEFORE">
			select product_seq.nextval from dual
		</selectKey>
		insert into product(pidx, upcg_code, down_code, pname, price,
		psaleprice, pqty, ppoint, pspec, pcontent, pdate, preadnum, plikenum)
		values(#{pidx},#{upcg_code},#{down_code},#{pname},{price},
		#{psaleprice},#{pqty},#{ppoint},#{pspec},#{pcontent:VARCHAR}, sysdate, 0, 0)
	</insert>
	
	<select id="getProdList" resultType="Product">
		select p.*,
		(select upCg_name from upCategory where upCg_code=p.upCg_code) upCg_name,
		(select downCg_name from downCategory where downCg_code=p.downCg_code) downCg_name 
		from product p order by pnum desc
	</select>
	
	<select id="selectByPspec" resultType="Product" parameterType="string">
		select * from product where pspec=#{value}
	</select>
	
	<select id="selectByIdx" resultType="Product" parameterType="_int">
		select * from product where pidx=#{value}
	</select>
	
	<select id="selectByCategory" resultType="product" parameterType="string">
	</select>
	
	<update id="updateProd" parameterType="Product">
	</update>
</mapper>